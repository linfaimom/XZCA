/**
 * Created by marcus on 2017/2/26.
 */
import React from "react";
import {grey800} from "material-ui/styles/colors";

const style = {
    backgroundColor: grey800,
    height: "64px"
};

const CAFooter = (props) => (
    <footer id={props.id} style={style}>

    </footer>
);

export default CAFooter;
